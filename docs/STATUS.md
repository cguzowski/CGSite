# Current status

Updated: 2026-09-24. Baseline branch: `modernizing-baseline`.

## Current decision

Website application stack: **TypeScript/Angular only**. Sections: **Home → About → Projects → Contact**. Spring Boot and blog scope are retired. This decision supersedes the former combined About/connect and blog/feedback layout.

Cleanup is implemented in application code and indexes. Content/design refresh remains separate from this foundation change.

## Delivery and observed tree

- iOS Projects focus-frame correction (September 24): section anchors remain programmatically focusable for navigation, but no longer paint the global focus outline around the full section. This removes the Projects-sized border iOS Safari showed after fragment navigation without changing focus movement or interactive-control focus indicators. The regression contract failed against the prior solid outline and passed after the correction; the focused App suite passed 14/14, the full Angular suite passed 67/67, and the production build passed with a 406.02 kB initial bundle plus the existing component-style budget warnings. Physical iOS verification remains outstanding. No deployment.

- Mobile collapsed section-label wrapping correction (September 24): the sticky section navigation toggle now keeps its current label on one line, preventing narrow Android viewports from splitting “Projects.” The focused rendered navbar test passed 1/1, the production build passed with a 405.98 kB initial bundle plus the existing component-style budget warnings, and `git diff --check` passed. Physical Android verification remains outstanding. No deployment.

- iOS marquee touch-drag correction (September 24): the marquee now uses a dedicated Touch Events gesture path when touch events are available instead of depending on iOS's cancellable Pointer Events stream. It tracks the initiating finger by identifier, pauses while held, moves horizontally in either direction, and resumes from the released position. A four-pixel direction lock prevents only horizontal gestures and releases primarily vertical gestures to native page scrolling. The iOS touch-drag contract failed against the pointer-only implementation and passed after the correction; the focused marquee suite passed 9/9, the full Angular suite passed 66/66, and the production build passed with a 405.96 kB initial bundle plus the existing component-style budget warnings. Physical iOS verification of this correction remains outstanding. No deployment.

- Mobile section navbar layout stability / F6 (September 24): keep mobile title hosts sticky with an unchanged flow height in every state; the collapsed/expanded controls overlay the retained title space. This removes the state-dependent height change that moved following content on entry/exit. A rendered 375px iframe regression reproduced a 27.19px entry shift before the correction and now passes through entry, expansion, collapse and exit. `CHROME_BIN=... npx ng test --watch=false --browsers=ChromeHeadless --progress=false --include=src/app/core/staticComponents/navbar/section-navigation.component.spec.ts` passed 1/1 after failing before implementation. `CHROME_BIN=... npm run test:ci` passed 64/64 outside the sandbox after the initial Chrome launch returned `spawn EPERM`; `npm run build` passed (404.76 kB initial bundle, existing component-style warnings). `git diff --check` passed. Physical iOS/Android scroll and compositor behavior remains unverified. No deployment.

- Projects walkthrough reorder (September 24): changed the three independent project sliders to Payment Incident AI Copilot, Atlas Capital Intelligence, then Self-hosted personal website. The focused Projects suite passed 6/6, the production build passed with a 404.81 kB initial bundle plus the existing component-style budget warnings, and `git diff --check` passed. No deployment.

- About marquee direct manipulation (September 24): replaced the desktop CSS-transform path and iOS-only scroll fallback with one native-scroll loop on every platform. Mouse, pen and single-touch pointer drags pause automatic movement, move the real horizontal position, wrap between buffered copies without a visible jump, and resume at the existing 56-second pace from the released position. Vertical page gestures remain available through `touch-action: pan-y`; the region is focusable and announces its drag affordance. Reduced motion remains manual-only. A rendered follow-up reproduced stationary automatic motion: per-frame subpixel increments were read back after browser rounding and lost. The loop now retains that fractional remainder; a rounding regression contract failed before the correction and passed after it. Browser verification measured 61.6px of uninterrupted automatic movement over 1.5 seconds, then a 312.8px drag followed by 48.8px of resumed movement over 1.2 seconds. The focused marquee suite passed 8/8, the full Angular suite passed 63/63, and the production build passed with a 404.81 kB initial bundle plus the existing component-style budget warnings. Physical touch-device verification remains outstanding. No deployment.

- Contact link contract reconciliation (September 22): updated the About and Contact regression expectations for the corrected `tel:+16468545750` phone URI and resume links that open in a new tab instead of downloading. The focused About and Contact suite passed 8/8 with ChromeHeadless outside the sandbox after the initial sandboxed Chrome launch returned `spawn EPERM`; the full Angular suite passed 61/61 and `git diff --check` passed. No deployment.

- About marquee iPhone sizing correction (September 20): replaced flex-basis-only highlight sizing with explicit responsive item widths and max-content group widths. This targets WebKit intrinsic sizing of the repeated strip, so the native scroll loop has the full content width instead of relying on flex-basis contributions. Added a 375px rendered-layout contract checking group extent and actual scroll positions across two complete cycles. The new contract passed against the prior code in Chrome (6/6 focused checks), so the reported iPhone failure was not reproduced locally and the suspected WebKit cause remains device-unverified. After the change, `CHROME_BIN=... npm run test:ci` passed 61/61 outside the sandbox (the initial sandbox launch returned `spawn EPERM`); `npm run build` passed with a 403.89 kB initial bundle and existing component-style budget warnings. `git diff --check` passed. Reduced-motion behavior is retained. No deployment.

- iOS Home fallback edge-to-edge correction (September 20): removed the shared content-image `max-width` constraint from the autoplay-blocked Fuji fallback and made the Home canvas explicitly span the viewport instead of inheriting the constrained `main` width. The static image retains the same portrait `cover` crop as the WebM and no longer leaves the 1rem side gutters visible in Low Power Mode. A viewport-coverage regression contract failed against the prior 16px inset and passed after the correction; the full Angular suite passed 60/60, the production build passed with a 403.87 kB initial browser bundle and the existing component-style budget warnings, and `git diff --check` passed. Physical iPhone Low Power Mode verification remains outstanding. No deployment.

- Media loading optimization / F7 (September 20): kept the 299,838-byte Fuji WebM unchanged, requested automatic preload, removed unused external Google font stylesheets, and replaced the 259,134-byte favicon with a pixel-identical 61,858-byte PNG. About requests its portrait only when visible. Each Projects deck waits until within 300px of the viewport before importing Swiper, requesting its active/adjacent images, and retaining visited images for reverse navigation. Pending decks reserve their dimensions without painting uninitialized stacked cards. Responsive image candidates account for cover cropping, device density/zoom and 15% resolution headroom; full-resolution originals remain available. The reproducible Pillow generator checks decoded pixels for every lossless WebP and favicon: 35 referenced images total 4,190,466 original bytes, selected full-size sources total 3,718,663 bytes (seven smaller lossless replacements), and the illustrative 800px-or-next-candidate total is 3,463,245 bytes. These aggregate figures include the iOS-only fallback and are not initial-transfer measurements. Browser checks against the production build at 1440×900/DPR1 and 375×812/DPR3 confirmed zero About/Projects photo requests and no Swiper chunk initially; scrolling loaded the portrait and nearby cards, Next/ArrowLeft navigation worked, reverse navigation retained images, and no page errors or horizontal overflow appeared. Screenshot review passed. The directive contract initially failed compilation before implementation; slider loading contracts failed 3/6 against the eager implementation. Focused media/About tests passed 6/6, then the full `CHROME_BIN=... npm run test:ci` passed 59/59 outside the sandbox after Chrome startup returned `spawn EPERM`. `npm run build` passed with one prerendered route, 403.76 kB initial JS/CSS (101.65 kB estimated transfer), and component-style budget warnings. `git diff --check` passed. Live-network timings, physical iOS and actual browser zoom were not measured in this slice. No commit, push or deployment.

- iOS marquee follow-up (September 20): the native-loop class now uses horizontal auto overflow with hidden scrollbars and removes the track transform completely. This targets WebKit painting of subsequent copies; desktop/Android CSS animation is unchanged. A 375px-wide rendered fixture verifies last-to-first spacing and the untransformed native-scroll contract (the latter failed before the CSS change). Physical iPhone reproduction/verification remains outstanding, so this is not confirmed device evidence. With `CHROME_BIN` set, `npx ng test --watch=false --browsers=ChromeHeadless --progress=false` passed 53/54 including all five marquee checks; the sole failure was the previously recorded Swiper lazy-chunk timeout. `npm run build` passed (393.26 kB initial bundle), with component-style warnings including the marquee at 2.12 kB. `git diff --check` passed. No deployment.

- iOS-only Home autoplay fallback (September 20): reverted the universal still-first Fuji behavior because it caused a visible image-to-video transition on desktop and ordinary mobile browsers. The original video remains the sole initial hero media everywhere. Only on iOS/iPadOS, after an explicit `play()` attempt is rejected, Angular now creates the static Fuji fallback and removes the blocked video from presentation; non-iOS platforms do not run the probe or request the fallback asset. This uses iOS plus autoplay rejection as the observable Low Power Mode signal because browsers expose no direct Low Power Mode API. The revised contract first failed at compile time, then its three normal/iOS/non-iOS cases passed; the full Angular suite passed 52/52 and the production build passed with a 393.03 kB initial browser bundle, one prerendered route and the existing component-style warnings. Physical iOS Low Power Mode verification remains outstanding. No deployment was performed.

- Home theme-control placement and presentation (September 20): the light/dark theme button no longer paints a circular border at any viewport size. At widths up to 30rem, the Home navigation uses two columns so the button sits beside the first-row Home link while About, Projects and Contact remain stacked beneath it. Wider layouts retain their existing navigation placement. The production build passed with a 391.28 kB initial browser bundle, one prerendered route and the existing component-style warnings; `git diff --check` passed. No deployment was performed.

- Mobile Home introduction line split (September 20): preserved the desktop two-line name/role presentation and made the role resolve into separate “Full Stack Engineer” and “& Applied AI” lines at widths up to 30rem. The mobile name uses a smaller responsive size and no-wrap treatment so “Christopher Guzowski” remains one line. The full Angular suite passed 47/47 and the production build passed with a 391.22 kB initial browser bundle, one prerendered route and the existing component-style warnings. No deployment was performed.

- Home Fuji credit placement (September 20): moved “Photos of Mt. Fuji by Christopher Guzowski” out of the upper-right introduction flow and anchored it to the visible WebM frame's lower-right inset for contained desktop and cropped portrait layouts. Also corrected the prior “Mt. Fujis” typo. The production build passed with a 390.92 kB initial browser bundle, one prerendered route and the existing component-style warnings. The isolated preview browser could not reach the local development server, so visual browser verification remains outstanding. No deployment was performed.

- iOS marquee paint/loop correction (September 20): retained an explicit zero-translation CSS start frame to avoid WebKit's initial `transform: none` paint failure, then replaced the transform animation on iOS/iPadOS with a native-scroll loop selected from the Apple platform identity rather than an unreliable CSS support probe. Four identical groups provide painted buffers on both sides; the iOS path moves between the middle copies by exactly one measured group over the same 56 seconds and never reaches a scroll-content edge. Other browsers retain the equivalent CSS animation; reduced motion still disables automatic movement and permits manual scrolling. The strengthened continuous-loop contract first failed at compile time and then passed 4/4. Two full-suite runs each passed 52/53, with only the unrelated existing lazy Swiper chunk timeout failing in different AppComponent tests. The production build passed with a 393.10 kB initial browser bundle, one prerendered route and the existing component-style warnings. Physical iOS verification of this strengthened follow-up remains outstanding. No deployment was performed.

- Mobile Home navigation tap feedback (September 20): suppressed the browser-painted tap-highlight rectangle on coarse-pointer viewports up to 48rem for the four hero navigation links and the light/dark theme button. The change does not alter hover or keyboard focus behavior. Styling-only correction; the production build passed with a 390.76 kB initial browser bundle, one prerendered route and the existing component-style warnings. No deployment was performed.

- Mobile section navigation outside-tap close (September 20): expanded About, Projects and Contact navigation now collapses when a pointer is pressed anywhere outside its component while interactions inside remain open. The regression contract failed against the previous behavior and then passed 8/8; the full Angular suite passed 45/45 after a sandboxed Chrome launch returned `spawn EPERM`. The production build passed with a 390.62 kB initial browser bundle, one prerendered route and the existing component-style warnings. No deployment was performed.

- SteamCup mobile swipe correction (September 19): added a passive Touch Events gesture path that keeps following the initiating finger even when a mobile browser cancels Pointer Events to perform native vertical scrolling. Touch movement now uses the same capped velocity calculation as mouse/pen movement, multi-touch list reordering does not switch the tracked finger, and Pointer Events remain the fallback where Touch Events are unavailable. The focused steam contract first failed at compile time, then passed 9/9; the full Angular suite passed 44/44 with ChromeHeadless outside the sandbox after a sandboxed Chrome launch returned `spawn EPERM`. The production build passed with a 390.36 kB initial browser bundle, one prerendered route and the existing component-style warnings. The isolated preview browser could not reach the local development server, so physical touch-device verification remains outstanding. No deployment was performed.

- Contact CI contract reconciliation (September 19): preserved the user-selected Contact presentation and changed its regression contract to match the existing order and copy: Location, Email, Phone, GitHub, Resume and LinkedIn, followed by the centered arrow link to `#home`. Link destinations, new-tab isolation, download behavior, icon/copy structure and the updated visible phone number remain unchanged. The full `npm run test:ci` suite passed 41/41 with Chrome outside the sandbox. The production build passed with a 390.39 kB initial browser bundle, one prerendered route and the existing component-style warnings; `git diff --check` passed. No deployment was performed.

- Automated static deployment / D1 (September 14): added a GitHub Actions workflow that runs locked dependency installation, the CI test suite and the production build for pull requests and `master` pushes on GitHub-hosted runners. Successful `master` checks upload only `angular/dist/angular/browser`; the repository-scoped Pi runner `CgRp-production` downloads the artifact and invokes a fixed, root-owned Pi helper. The ARM64 runner is registered with `self-hosted`, `Linux`, `ARM64` and `cgsite-production` labels, installed as an enabled/running systemd service under the dedicated unprivileged `cgsite-runner` account, and visible idle in GitHub. The root-owned helper stages commit-addressed releases below `/var/www/cguzowski.com/angular/releases`, atomically switches the existing `browser` path, validates Nginx and a local production-Host request, restores the previous target on verification failure, retains three releases and provides a rollback command. Its Bash syntax passed on the Pi; the helper is root-owned mode 0755, its validated sudoers entry is root-owned mode 0440, the runner cannot write the Nginx document root, and outbound GitHub HTTPS succeeded. GitHub's `production` environment exists with selected branch/tag restrictions and exactly one allowed branch rule, `master`; it has no secrets or variables. Workflow YAML parsed with both jobs; `npm run build` passed with a 390.25 kB initial browser bundle and the existing component-style warnings; `git diff --check` passed. `npm run test:ci` ran 41 tests: 39 passed and the same two pre-existing Contact copy/order expectations failed. Two real `master` workflow runs completed with failure at `Run tests`; GitHub marked `Deploy production` skipped, confirming the gate prevented the Pi from changing. The current live `browser` directory remained untouched. First successful workflow deployment and rollback drill remain unverified.

- SteamCup mobile interaction correction (September 14): removed the touch-pointer exclusion that made the steam ignore mobile input, added a short directional gust for taps, and reset tracking at pointer end/cancellation so separate gestures do not connect. The cup interaction surface accepts pointers with `touch-action: pan-y`, while its expanded canvas and SVG remain non-intercepting; vertical native scrolling is preserved. The focused contract failed at compile time before implementation and passed 6/6 afterward. The full Angular suite ran 41 tests: 39 passed, with only the same two pre-existing Contact copy/order expectations failing. Production build passed with a 390.25 kB initial browser bundle, one prerendered route and the existing component-style warnings; `git diff --check` passed. Physical touch-device verification remains outstanding. No deployment.

- Modern portfolio production deployment (September 14): deployed the verified `angular/dist/angular/browser` build to the Raspberry Pi Nginx document root `/var/www/cguzowski.com/angular/browser` through Raspberry Pi Connect. The 4,567,534-byte release archive matched SHA-256 `2dd354a92230a206d5c3d98db7b6a4d52ce539f898adc604d21b1b9941e3add3`; the staged entry point and `nginx -t` passed before the atomic directory swap. A local Nginx request with the production Host header returned the new Payment Incident AI Copilot content, and the public `https://cguzowski.com/` origin returned the modern Home, About, three Projects walkthroughs, Contact and 2026 footer. The previous site remains recoverable at `/var/www/cguzowski.com/angular/browser.backup-20260914-1454`. The temporary transfer tunnel was stopped after verification.

- Payment Incident AI Copilot walkthrough wiring (September 14): restored the Projects collection property accidentally replaced while adding the third slide set, then registered the new walkthrough before Atlas and corrected its first image path to the available `copilot0WorkQue.jpg` asset. The focused Projects suite passed 6/6 and the production build passed with a 388.36 kB initial browser bundle and existing component-style warnings. The full suite ran 39 tests: 35 passed; one existing Swiper lazy-chunk timeout and the same pre-existing Contact expectation drift accounted for four failures. Several Copilot cards still have placeholder copy or missing alternatives and require content completion before P1 is complete. No deployment.

- Projects heading-to-slider spacing correction (September 13): removed the fixed 8.75rem project-heading reserve and the slider's redundant 24px top padding, leaving a deliberate 0.75rem gap between each project summary and its active slide title. The two project columns and card alignment remain unchanged. Production build passed with a 384.88 kB initial browser bundle, one prerendered route and the existing component-style warnings. Desktop browser review confirmed the marked empty band is gone; at the 375px viewport override the measured summary-to-title gap is 12px and document width equals viewport width. Styling-only correction; tests were not rerun. No deployment.

- Home introduction alignment correction (September 13): restored right alignment for the upper-right Christopher Guzowski name, role and Fuji credit block by correcting the shared Home placement rule. Positioning, navigation and responsive wrapping are unchanged. Production build passed with a 385.00 kB initial browser bundle, one prerendered route and the existing component-style warnings; `git diff --check` passed. Browser review at the default desktop viewport and 375×812 confirmed all three lines align right while the block remains inside the Fuji frame without colliding with the upper-left navigation. Styling-only correction; tests were not rerun. No deployment.

- Projects multi-walkthrough container / Atlas slider (September 13): refactored the single ambiguous `projects` card collection into an explicit Projects container containing reusable project walkthroughs and slide collections. The existing infrastructure story is now named “Self-hosted personal website” in the UI and `personalWebsiteInfrastructureSlides` in code. Added a second, independently controlled nine-slide Atlas Capital Intelligence walkthrough from the supplied `ACIPics` assets, with useful alternatives and copy limited to visible UI evidence. Atlas is labeled as an implemented portfolio-analysis frontend while Alpaca retrieval, caching and chatbot integration remain planned. The focused Projects contract first failed at compile time against the old one-slider API, then passed 8/8. The full Angular suite ran 38 tests: 36 passed, while the same two unrelated Contact copy/order/back-to-top expectations failed. Production build passed with a 385.00 kB initial browser bundle, one prerendered route and the existing component-style warnings. Browser review confirmed two aligned columns at the default desktop viewport, a single-column layout at 375×812, no visible horizontal clipping, and independent Atlas navigation from slide 1/9 to 2/9. Physical touch and OS reduced-motion remain unverified. No deployment.

- F6 flat-oval follow-up (September 13): the active mobile section navigation no longer paints a full-width strip. Its sticky host shrink-wraps a centered, fully rounded surface around the collapsed dotted title and expands to the width of the four-link row. The side areas remain transparent, while 44px minimum control height and current-link treatment remain intact. Browser review at 375x812 and 320x700 confirmed both states remain centered and unclipped. Styling-only follow-up: interaction tests were not rerun; `npm run build` passed with a 379.94 kB initial browser bundle, one prerendered route and the existing component-style warnings; `git diff --check` passed. No deployment.

- F6 final verification: `npm run test:ci` ran 37 tests: 35 passed including the final navigation contracts, and the same two pre-existing Contact copy/order/back-to-top expectations failed. Chrome required execution outside the sandbox after `spawn EPERM`. `npm run build` passed with a 379.65 kB initial browser bundle, one prerendered route and the existing component-style warnings; `git diff --check` passed.

- Mobile section navigation / F6 (September 13): at <=48rem, About, Projects and Contact headings become section-constrained sticky bars with the requested ordered dots/current-title pattern only when each heading reaches the viewport top. Upcoming headings remain regular centered section titles, so the current sticky bar never competes with a second navigation-looking title. Tapping reveals all four native section links; the current section is bold/underlined. Selection defers collapse until the browser performs its native fragment action; Escape collapses and returns focus. Leaving a section also collapses its menu. Desktop headings and Home remain unchanged. The original interaction contract first failed 1/6, then passed 6/6; the single-bar regression first failed 1/7, then passed 7/7. Browser checks at 375x812 confirmed Projects as the sole sticky dotted bar while Contact remained a regular centered title, followed by Contact becoming the sole bar after scrolling. Expanded links, keyboard navigation, 1280x900 desktop behavior, actual touch and 200% zoom were not rechecked for this follow-up. No deployment.

- Both Home corners retained (September 13): supersedes the navigation flow correction below. Navigation remains absolutely anchored at the Fuji frame's upper left at all widths, while the introduction remains upper right. Up to 60rem navigation uses two columns; up to 30rem links and theme control stack vertically. The introduction reserves horizontal room for navigation and wraps rather than overlapping. Browser review at 375×812 and 800×600 confirmed both top anchors and separated content. `npm run build` passed with existing style-budget warnings; `git diff --check` passed. Styling-only change; unit tests not rerun.

- Mobile Home top-right correction (September 13): removed the responsive 6rem/8rem introduction offsets. The name/role/photo-credit block retains the Fuji frame's 1rem top/right inset; at widths up to 60rem navigation flows beneath it, so navigation height cannot push the introduction down. Header markup now lives inside Home to support natural flow, with desktop absolute placement retained. Styling/layout verification: `npm run build` passed (375.50 kB initial browser bundle, one prerendered route; existing style warnings), and `git diff --check` passed. Browser visual review and tests were not rerun for this correction. This supersedes the mobile placement described in the prior overlay entry.

- Home overlay placement swap (September 13): moved the navigation and light/dark theme control from the upper right to the upper left of the Fuji frame, and moved the Christopher Guzowski title/role/photo-credit block to the upper right. The introduction shrink-wraps its content against the Fuji frame's 16px right inset while every line remains left-aligned. At mobile widths the navigation uses two rows—Home/About followed by Projects/Contact—with the theme control beside it, while the complete name remains on one line at the upper right beneath the taller navigation. Production build passed with one prerendered route and a 375.56 kB initial browser bundle; existing component-style warnings remain below their error budgets. Browser review at 1280x720 and 375x812 confirmed the desktop single-row and mobile two-row navigation, the single-line right-anchored name, and no horizontal overflow.

- CupSteam rim coverage and height adjustment (September 13): retained the ten central steam spawn points and added one dedicated spawn point at each cup-rim edge, for twelve total. Plume rise distance is 50% taller than the immediately preceding 4/3-tuned version, and upper simulation padding increased from 150 to 225 units so the new height remains unclipped at desktop and mobile sizes. The focused steam contract failed before implementation and passed 4/4 afterward. Production build passed with one prerendered route and a 375.34 kB initial browser bundle; `git diff --check` passed. The full Angular suite ran 35 tests: 33 passed, while the same two unrelated Contact tests failed against pre-existing Contact markup/test drift. Browser review at 1440×900 and 375×812 confirmed steam spanning the full rim and rising above the cup without horizontal overflow.

- About desktop composition refinement (September 13): reduced the biography/coffee grid gap from a 3rem to 2rem maximum and lowered the coffee by 2.5rem. The existing <=48rem layout explicitly resets the coffee offset, so the mobile portrait/cup row is unchanged. Production build passed with one prerendered route and a 375.34 kB initial browser bundle; About styles remain at the 4.00 kB error ceiling. Browser review at 1167x900 confirmed the wider biography column and lower cup, while 375x812 retained the original mobile alignment.

- Shared typography refresh (September 13): replaced the Roboto/Arial declaration with a dependency-free modern system font stack and raised the responsive base from a fixed 16px to 17px at 375px and 18px on desktop, with a 1.62 line-height. About biography now reaches 19.84px/33.73px at the inspected 1280px viewport, closely matching the supplied inspiration's measured 20px/34px About prose. Section headings, Home title/copy/credit, About orbit and group labels, marquee text, project title/card copy, Contact introduction/notes, and footer received proportionate increases while preserving hierarchy. The first build correctly failed because About CSS exceeded its 4 kB error budget by 28 bytes; consolidating line-height and removing a redundant max-width brought it back to exactly 4.00 kB. The production build then passed with one prerendered route and a 375.36 kB initial browser bundle; existing component-style warnings remain below their error budgets. Desktop browser review confirmed 18px body text, ~20px About prose and 16px marquee text. At 375×812, body text is 17px, marquee text is 15px, document width is 360px within the viewport, and visual review of Home, About, Projects and Contact confirmed readable wrapping without horizontal clipping. The full Angular suite ran 33 tests: 31 passed, while the same two unrelated Contact tests failed against pre-existing Contact markup/test drift. Actual 200% zoom remains unverified.

- About professional highlights marquee (September 13): added ten paired symbol-and-text highlights directly beneath the About profile links. The standalone Angular component uses native colored emoji plus an original inline drone SVG, shows five items across at the 1088px content width, and about three items with faded edge context at 375px. The seamless 56-second loop runs continuously with no pause state, control, or hover behavior. Reduced-motion removes animation and duplication while retaining manual horizontal access to all items. The public education text remains accurate as “Georgia Tech OMSCS · Starting Spring 2027.” A focused placement/content contract failed 1/4 before implementation, then passed 4/4. After all pause-related code was removed, the production build passed with a 375.07 kB initial browser bundle and the marquee no longer produced a component-style budget warning. The full Angular suite previously ran 33 tests: 31 passed, while two unrelated Contact tests failed against pre-existing Contact markup/test drift. Desktop browser review measured five items across (192px each in a 1088px marquee), and 375×812 review measured 90.74px items in a 328px marquee with no document overflow. OS-level reduced-motion remains unverified.

- Contact spacing refinement (September 13): reduced the large vertical gaps beneath the Contact introduction, between the two desktop icon rows, before the Back to top control, and beneath the Contact content by approximately 30%. Horizontal grid spacing, responsive column counts, copy and link behavior are unchanged. Styling-only follow-up: production build passed with one prerendered route and a 366.14 kB initial browser bundle; existing component-style warnings remain below their 4 kB error budgets.

- Contact direct connections (September 13): replaced the former Material-icon list with an original responsive icon-and-text composition. Location, email, LinkedIn, GitHub, resume and phone now use semantic direct links, with map/profile targets isolated in a new tab, `mailto:`/`tel:` protocols, the existing resume download, and a centered pill-style Back to top control with an original upward arrow. Each icon sits above its paired text with a three/two/one-column desktop/tablet/mobile layout and restrained About-matched hover/focus treatment. Pin-drop, mail and phone are new original local SVGs; Resume, GitHub and LinkedIn reference the exact existing About SVG assets without copies. The initial C1 focused contract failed 2/3 expectations against the previous component, then passed 3/3. The arrow follow-up added a focused regression that failed 1/3 before implementation, then passed 3/3; the full Angular suite passed 32/32 outside the sandbox after a sandboxed Chrome launch returned `spawn EPERM`. Production build passed with one prerendered route and a 366.00 kB initial browser bundle; Contact CSS is 3.60 kB and produces a 2 kB component-style warning while remaining below the 4 kB error budget. Desktop and 375×812 browser review confirmed the intended spacing, rendered icons, centered return link and no horizontal overflow. Actual 200% zoom remains unverified.

- Shared footer (September 13): added the requested “Designed & Built by Christopher Guzowski © All Rights Reserved 2026” footer below Contact. It uses a subtle theme-adaptive gray, centered small text, and a section-aligned top rule so it remains visible without competing with section content. The new footer contract is covered by the Angular suite, which passed 30/30 with Chrome outside the sandbox. Production build passed with one prerendered route and a 404.99 kB initial browser bundle; the shared shell (2.23 kB), Projects (2.51 kB), and About (3.97 kB) component styles retain warnings below their 4 kB error budgets.

- Shared spacing refinement (September 13): reduced the shell's section padding from `clamp(2.5rem, 6vw, 5rem)` to `clamp(2rem, 4vw, 3.5rem)` and section-heading bottom spacing from 2rem to 1.5rem, tightening About, Projects and Contact consistently. About's internal grid gap now tops out at 3rem instead of 5rem, and the Skills/Core Tech/Hobbies block no longer has horizontal top/bottom borders or their extra block padding. Styling-only correction: production build passed with one prerendered route and a 404.46 kB initial browser bundle; existing About and Projects component-style warnings remain below their error budgets. Desktop and 375×812 browser review confirmed the denser rhythm, removed internal rules, retained section boundaries and no horizontal clipping. Actual 200% zoom remains unverified.

- About professional snapshot (September 13): added Skills, categorized Core Tech and Hobbies between the biography/coffee composition and profile links. Copy normalizes professional terminology including full-stack development, systems design, TypeScript, PostgreSQL, APIs, JWT and Cloudflare. Desktop uses a restrained three-column layout with extra width for Core Tech; at <=48rem the groups stack in source order and the profile links follow. A new placement/content contract failed against the prior markup, then the full Angular suite passed 29/29. Production build passed with one prerendered route and a 404.54 kB initial browser bundle; the About component style remains at the existing 4 kB error ceiling and produces a 2 kB warning. Browser review at the default desktop viewport and 375×812 confirmed readable hierarchy, correct ordering and no horizontal clipping. Actual 200% zoom remains unverified.

- Default dark theme (September 13): first-time visits now start in dark mode. The raw page body carries the dark class so the initial render does not begin light, and `ThemeService` treats a missing saved preference as dark while continuing to honor a visitor's explicitly saved light preference. A focused regression failed against the prior light default, then theme tests passed 3/3 and the full Angular suite passed 28/28. Production build passed with one prerendered route and a 402.41 kB initial browser bundle; existing About and Projects component-style warnings remain below their error budgets. Fresh-origin browser review confirmed the Sun/“Use light theme” control and dark styling across the About content.

- About profile links (September 13): added a compact three-column icon-and-label row beneath the portrait/biography and coffee composition for Resume, GitHub, and LinkedIn. The initial bordered card treatment was removed after user review. Links now have no button surface or border, illuminate with a restrained color/glow on hover or compact keyboard focus, and remain in one row at 375px. All three icons are original local SVG assets: a solid folded document, a developer-cat silhouette, and a rounded professional “in” mark. The briefly introduced Font Awesome-derived glyphs and their license were removed when the original set replaced them. Resume downloads the existing public PDF; verified profile destinations open in a new tab with safe opener isolation. The new contract failed against the previous markup, then the full Angular suite passed 26/26. Production build passed with one prerendered route and a 402.40 kB initial browser bundle; About CSS is 3.49 kB and produces a 2 kB component-style warning while remaining below the 4 kB error budget. Desktop and 375×812 browser review confirmed the compact row, familiar shapes, tight focus target, and no horizontal overflow; the final original icons received an additional desktop browser review. Physical touch and OS reduced-motion remain unverified.

- Projects slider title/control refinement (September 13): each active card now has a bold, underlined, fixed-size title link above the deck with an external-link icon and a verified destination. The title is removed from the card body, the play triangle uses symmetric geometry so it is centered in its circle, and the numeric position remains available to assistive technology without being rendered visually. Full `npm test -- --watch=false --browsers=ChromeHeadless --progress=false` passed 25/25, and desktop plus 375×812 browser review confirmed the link hierarchy, centered play control, responsive containment, and absent visual counter. Production build passed with one prerendered route and a 399.49 kB initial browser bundle; the slider stylesheet is 2.51 kB and produces the existing 2 kB component-style warning while remaining below the 4 kB error budget. Physical touch and OS reduced-motion remain unverified.

- Projects card-slider foundation (September 13): removed the former Raspberry Pi placeholder card and integrated the supplied standalone Swiper Element card slider. Five supplied hosting-story images appear in filename order: physical Raspberry Pi, Raspberry Pi Connect, registrar domain list, Cloudflare domain overview, and the live mobile site. Cards have concise visible-evidence text and useful alternatives without repeating the account email shown in a screenshot. Drag/swipe, previous/next buttons, Left/Right Arrow keys, explicit play/pause, end-state disabling, polite position feedback, timer cleanup, and reduced-motion zero-speed behavior are retained. This is reusable interaction groundwork, not completion of the planned Copilot or Atlas screenshot stories.
- Projects verification: two new parent integration expectations failed against the old placeholder (2 failed, 1 passed), then focused Projects tests passed 5/5. Full `npm run test:ci` passed 23/23 with `CHROME_BIN` outside the sandbox after the sandboxed Chrome launch returned `spawn EPERM`. `npm run build` passed with one prerendered route, 398.19 kB initial browser bundle, and a separate 183.92 kB Swiper lazy chunk. Browser review at the normal desktop viewport and 375×812 confirmed the card stack, readable controls, responsive containment, and Next advancing position/content from 1/5 to 2/5. Physical drag/touch and OS reduced-motion remain unverified.

- A2 steam length adjustment: plume rise distance increased by 4/3 (about 33%) at all viewport sizes; rise speed and cup dimensions retained. Existing canvas padding accommodates the longer plume. Styling-only tuning: no new tests; production build passed after sandbox spawn EPERM retry outside the sandbox. Visual review of the new length remains pending.

- A2 steam overflow correction (September 13): cup host now allows foreground overflow, and the canvas adds 300 simulation units per side plus 150 above/below. Drawing/clearing uses translated bounds, retaining cup coordinates and half-resolution rendering. Passive document pointer tracking follows displaced steam outside the slot; the illustration ignores hit testing so text selection and scrolling pass through. Fuji gate unchanged. Two desktop/mobile bounds regressions failed before the fix; `npm run test:ci` passed 19/19 with CHROME_BIN and outside-sandbox Chrome, `npm run build` passed, and `git diff --check` passed. Browser checked 1440x900 and 375x812: canvas extends beyond both slot edges, host overflow is visible, no horizontal page overflow; mobile-size pointer swipe visibly dispersed steam. Physical touch-device and OS reduced-motion testing remain unverified.

- A2 (September 13): About now follows supplied desktop/mobile coffee references. Removed the crossed-out Skills/Aspirations/Hobbies lists; existing biography and portrait ring retained. Supplied SteamCup SVG and simulation ported to a transparent Angular component. Desktop places cup beside biography; at <=48rem portrait/cup share a row above biography. Deferred chunk waits for About visibility and Fuji loadeddata, with already-ready/error/reduced-motion handling. Steam pauses offscreen/hidden, respects reduced motion, and preserves native touch scrolling. Only About application files changed for A2.
- A2 checks: five loading contract tests initially failed before implementation; corrected a constructor mock during the green run. `npm run test:ci` in `angular/` passed 17/17 (CHROME_BIN set; outside-sandbox retry needed for Chrome spawn EPERM). `npm run build` passed, one prerendered route, 377.89 kB initial bundle and separate 8.08 kB steam chunk. Browser screenshots at 1440x900 and 375x812 confirmed composition and visible steam, with document width within viewport. Reduced-motion/offscreen cleanup reviewed in code; OS emulation and actual 200% zoom remain unverified. No commit, push or deployment.

- Home edge correction: feather stops reduced from 4% to 0.4% (one-tenth). Introduction and header offsets now share the video's natural aspect ratio and visible bounds, keeping them inside the image instead of in the side bands. Edge inset reduced from 1.5rem to 1rem. Production build passed; visual review remains outstanding.

- Home presentation update: feathered video edges using an intersected CSS mask; wide video bounds follow its loaded aspect ratio so feathering reaches the actual image edges. Navigation is upper right with the theme button outside the navigation landmark to its right. The requested name, role and photo credit are upper left, flowing below controls on narrow screens. Production build passed; visual browser review remains outstanding.

- Mobile framing: square/portrait viewports (`max-aspect-ratio: 1/1`) now use centered `cover` to fill the background height and crop the landscape video's sides. This replaces the ineffective 40rem alignment-only override. Wide viewports retain `contain`. `npm run build` passed; visual framing awaits user review.

- H0 desktop framing correction: centered cover cropped the summit and reflection in the user's wide screenshot. Wide viewports use `object-fit: contain` with centered alignment to preserve the entire frame without distortion. Unfilled space shows the page background; navbar overlay remains unchanged.

- H0 follow-up: header now overlays the Home video with transparent, absolute positioning instead of occupying a row above it. Home content retains clearance for the desktop navbar and its two-row mobile layout; background starts at the page top. Navbar controls and markup unchanged.

- H0 (September 13): supplied Fuji WebM copied unchanged into `angular/public/videos/` and added as a muted, looping, inline Home background. Reference CSS/JS uses full width, viewport height with a 500px minimum, cover sizing and top-center positioning; the Home background follows those rules. Reduced-motion hides the video. Navbar and other section files were not edited for H0. `npm run build` passed (369.18 kB initial bundle); `npm run test:ci` passed 12/12 after retrying outside the sandbox because Chrome spawn was denied; `git diff --check` passed. Desktop/mobile visual playback and OS reduced-motion remain unverified.

- B0: documentation baseline updated for the new direction.
- F1: historical build/test, hosting and performance audit preserved in [baseline](BASELINE.md).
- F2: former three-section shell implemented and tested on September 9; now superseded by F3.
- F3: cleanup implemented and tests/build passing; actual 200% browser zoom remains unverified. H1/A1/C1, project walkthroughs and release work remain planned.
- F4: restrained motion restoration implemented. The theme action is a Moon/Sun icon with a one-second color transition. An aria-hidden Plan/Build/Test/Learn ring advances between the four cardinal positions around the About portrait, rotating for one second and pausing for two, while biography copy retains circular text flow. Reduced-motion disables both transitions/animation.
- Spring Boot files are deleted in the working tree; do not restore them.
- Blog component/model/service, associated tests, non-delivering form/upload scaffold, three empty component stubs and superseded planning text files removed.
- Shell now renders Home, About, Projects, Contact with matching native navigation. Single page h1 lives in the new minimal Home component; About no longer duplicates it.
- Contact contains the existing direct links; #connect is retained as a legacy target inside Contact. #blog is retired. Existing contact details/resume and biography still need C1/A1 reconciliation.
- Projects now has a reusable multi-walkthrough container, the self-hosted website story, and an Atlas screenshot story. The planned Copilot walkthrough and architecture interaction remain incomplete; Atlas's unfinished integrations are labeled planned.
- Angular production configuration includes prerender/SSR. Previous read-only audit found static Nginx hosting plus cloudflared on Pi 3 Model B. No deployment changes made.

## User-confirmed content truth

- Focus: full-stack development + AI integration engineering.
- Georgia Tech OMSCS: admitted, starting Spring 2027; do not imply current enrollment or completed degree.
- Feature Copilot before Atlas, using screenshot/text stories rather than live applications.
- Copilot: RAG works with a real local model; mock service evidence and report generation unfinished.
- Atlas: frontend UI/UX exists; Alpaca retrieval/caching and chatbot unfinished.
- Transcript is private context only, never public site content or an AI corpus input.

These corrections override broader claims in old project READMEs/resume. Verify new claims before publication. The website stack decision does not change the featured projects' actual stacks. Do not depend on neighboring project directories at build/runtime.

## Next: H1 / A1

Develop Home content/design and refresh About. Contact direct links/resume are reconciled for C1. These section tasks can proceed independently with shared-shell edits coordinated. Keep the cleaned four-section contract and do not restore retired code.

Later decisions: accurate public resume/profile destinations, screenshot assets/capture dates, Contact delivery requirements, deployed revision and release/rollback procedure. Website source is `https://github.com/cguzowski/CGSite`; the general profile link is separate.

## Verification limits

- September 9 F2: 14/14 tests and production build passed; initial bundle 521.76 kB with the existing 500 kB warning. Those results describe the previous layout, not the new direction or current deletions.
- Previous browser checks: 375×812, 1440×900 and 720×450 equivalent reflow without clipping; keyboard anchors/skip link and theme toggle worked. Actual 200% zoom and OS reduced-motion emulation remain unverified.
- Installation previously reported 72 dependency advisories; reachability/upgrade scope remains untriaged (M1).
- F3 TDD: new order/placement tests failed before implementation (2 failures); cleaned suite passed 11/11. Three obsolete blog/form smoke tests were deleted, not skipped.
- Final production build passed with one prerendered route and initial bundle about 366 kB, below the unchanged 500 kB warning budget. Dependency versions unchanged; baseline-browser-mapping still reports stale compatibility data.
- F4 verification: `npm run test:ci` passed 12/12 and `npm run build` passed with one prerendered route and a 368.54 kB initial bundle. Browser inspection confirmed the theme background was still interpolating at 500 ms and reached its final value after one second; the control label/icon changed with the theme. At 375×812, document width stayed within the viewport and the portrait returned to normal flow. The portrait word animation uses a 12-second cycle containing four one-second quarter-turns and four two-second pauses. Reduced motion was verified from the CSS media rules, not by OS emulation.
- Browser reflow checked at 375×812, 1440×900 and 720×450: no horizontal overflow or section clipping. Keyboard Contact link focused the contact section. 720px is an equivalent reflow check, not actual browser zoom.
- Local Markdown links and diff checks are validated at handoff.
- No production deployment or remote changes.

Keep this file a current snapshot. Roadmap owns acceptance criteria; section indexes own paths.
